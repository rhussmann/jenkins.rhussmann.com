FROM jenkins

# TODO: consider extracting plugins to a plugins file after we're up and running
#COPY plugins.txt /usr/share/jenkins/ref/
#RUN /usr/local/bin/plugins.sh /usr/share/jenkins/ref/plugins.txt
